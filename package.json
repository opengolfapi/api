{
  "name": "@opengolfapi/api",
  "version": "1.0.1",
  "description": "Free REST API for the OpenGolfAPI dataset (14,708 courses), deployed on Cloudflare Workers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.51.0",
    "@supabase/supabase-js": "^2.0.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@vitest/coverage-v8": "^4.1.5",
    "typescript": "^5.0.0",
    "vitest": "^4.1.5",
    "wrangler": "^3.0.0"
  }
}